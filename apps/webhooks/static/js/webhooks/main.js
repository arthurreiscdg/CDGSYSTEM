/**
 * main.js - Arquivo principal que carrega todos os módulos
 */

// Executar quando o DOM estiver pronto
document.addEventListener('DOMContentLoaded', function() {
    // Inicializar o aplicativo
    WebhookApp.Core.init();
});